name: CI & Release

on:
  push:
    branches: [ "main" ]
    tags: [ "v*" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: Check, Format & Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - name: Check format
        run: cargo fmt -- --check
      - name: Clippy
        run: cargo clippy -- -D warnings

  test:
    name: Test
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4
      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
      - name: Run tests
        run: cargo test --verbose

  build:
    name: Build Release
    needs: [check, test]
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            suffix: linux-amd64
            use_cross: false
          - os: ubuntu-latest
            target: aarch64-unknown-linux-gnu
            suffix: linux-arm64
            use_cross: true
          - os: macos-latest
            target: x86_64-apple-darwin
            suffix: macos-amd64
            use_cross: false
          - os: macos-latest
            target: aarch64-apple-darwin
            suffix: macos-arm64
            use_cross: false
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install Cross
        if: matrix.use_cross
        run: cargo install cross --git https://github.com/cross-rs/cross.git

      - name: Build Release
        shell: bash
        run: |
          if [ "${{ matrix.use_cross }}" = "true" ]; then
            cross build --release --target ${{ matrix.target }}
          else
            cargo build --release --target ${{ matrix.target }}
          fi

      - name: Package
        shell: bash
        run: |
          BINARY_PATH="target/${{ matrix.target }}/release/awsp"
          # Handle cases where target directory is just target/release for native builds
          if [ ! -f "$BINARY_PATH" ]; then
            BINARY_PATH="target/release/awsp"
          fi
          
          mkdir -p dist
          cp "$BINARY_PATH" dist/awsp
          cd dist
          tar -czf ../awsp-${{ matrix.suffix }}.tar.gz awsp

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: awsp-${{ matrix.suffix }}
          path: awsp-${{ matrix.suffix }}.tar.gz

  release:
    name: Create Release
    needs: build
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            awsp-linux-amd64/awsp-linux-amd64.tar.gz
            awsp-linux-arm64/awsp-linux-arm64.tar.gz
            awsp-macos-amd64/awsp-macos-amd64.tar.gz
            awsp-macos-arm64/awsp-macos-arm64.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
